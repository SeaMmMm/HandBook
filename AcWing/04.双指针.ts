function dpointer(arr: number[]) {
  for (let i = 0, j = 0; i < arr.length; i++) {
    while (j <= i && check(i, j)) j++

    // 题目的具体逻辑
  }

  // 判定j是否满足某种条件
  function check(i: number, j: number): boolean {
    return false
  }
}
